# P5 blocking performance gates (queued)

Implementer ownership when assigned: tools/perf/**, package scripts only perf-related, CI dedicated perf job, web isolated performance spec/config/fixture only. Production optimization requires primary-reviewed measured evidence and separately assigned paths. Do not change budgets to fit results.

## Required budgets from PLAN §6

- 1000-entry API listing warm p95 <100ms, cold p95 <400ms.
- 10000-entry UI interactive <500ms AFTER data available, both list and grid. Measure browser rendering/interaction, not API latency. Virtualized bounded DOM; actual scroll/select/open readiness. Use production build, deterministic machine-readable marks from harness/read-only browser DOM observations; avoid app instrumentation unless approved. Do not replace with server list10k latency (informational only).
- Download payload throughput via API >=90% direct same SFTPGo source.
- 200 small files upload through app complete <30s; six streams (existing queue). No errors/lost files.
- Search real Postgres25k indexed files p95<300ms via authenticated API with correctly configured scope. Seed deterministic realistic mixed names/content, index stats/ANALYZE, validate result correctness and account boundary. Declare semantic embedding configuration and measure actual configured path; don't claim a fake embedding model measures model latency. Agree exact fixture before implementing.

Current runner lacks UI/search, measures cold only once, allows skipped/NaN/error cases and informational default. Close these validity gaps. Strict full gate must have every required finite metric, enough repeated observations (>=20 cold unique paths, >=100 warm/search after explicit warmup), zero failed requests, no skipped scenario. Diagnostic --only/--quick remain clearly non-gating and cannot produce full-gate success. Add tests for missing/nonfinite/error data and strict CLI behavior.

Download compare completed transfers (not HTTP headers or partial/aborted samples), verify full expected payload size, alternate direct/API order across multiple samples after warmup. Bound memory: stream/drain, don't arrayBuffer large files. Report bytes/s and ratio. Use existing seeded512MiB data if feasible; do not shrink merely for passing. No concurrency with unrelated heavy suites for final measurements. Record environment and raw metrics in ignored results artifact.

Primary runs dedicated quiet final gate and reviews results. If throughput fails, evaluate planned authenticated Caddy proxy only after measuring; no leaking upstream credential/signedURL secrets and no public bypass. Production optimization separately specified. CI perf job required and cannot silently skip failures or required scenarios; runner and pure modules appropriate coverage, Node24/pnpm10.11. Full root lint/typecheck/coverage and affected integrations/browser after code integrates.

## Launch specification (2026-09-07)

Assigned checkout `.worktrees/p5-performance`, branch `codex/p5-performance`.
Ownership: tools/perf/**; new apps/web/perf/** (production browser measurement helpers only), .github/workflows/performance.yml. Root/apps/web package manifests and lockfile are held by office worker: propose needed script/dependency additions, wait for parent refresh before editing those files. No API production/auth/scope/composition edits. No testkit modifications without scope extension. Existing fixture utilities may be imported. Parent owns integration and final quiet measurement; worker must run a diagnostic full harness to prove execution, report contention honestly, then final parent gate after other heavy jobs stop.

Search fixture decision: full gate measures the configured default hybrid path with real multilingual-e5-small TEI CPU, real Postgres384-dimensional vectors and actual authenticated API. At least25,000 indexed files/chunks in the measured identity scope, plus separate forbidden identity records as leakage sentinels. Deterministic mixed names/content; prepare embeddings from repeated content templates through actual TEI and replicate those vectors across fixture documents (report repetition). Queries must exercise filename, FTS and semantic signals; validate expected hits and forbidden absence. No fake embedding responses. Model assets may be cached outside committed output; use existing project's TEI image/model, no unsolicited model migration. Full gate cannot silently degrade/disable semantic search. Report keyword diagnostic separately if useful.

Scope verification integration is being implemented in another queued chunk: keep fixture inputs explicit (root name, home template, root directory listing service). Do not bypass it with injected production authorization. Before foundation integration, real SFTPGo directory contents can seed mirrored read-only fixture files for the real Python directory endpoint; after parent refresh, use same mapped root and actual indexer endpoint. Ask for narrow testkit fixture extension if sharing the disposable container's filesystem requires it. Avoid seeding25k files one HTTP request at a time if a deterministic fixture archive/bind mount offers equivalent real storage; retain real user API for measured operations.

UI measurement: start isolated production Next server with the fixture API rewrite configured at build time, real login cookie and real10k listing. Browser response event/data arrival starts timing; wait for meaningful ready state (visible rows, selection/keyboard response, scroll renders a distant row), verify bounded mounted row count in list and grid. Use Playwright through existing web dependency. Build output/runtime remains ignored. Never use timing assertions that start after rendering completed.

Read WORKING.md and role instructions. Node24/pnpm10.11 paths supplied by parent. Not alone in codebase; never revert others. Work only absolute assigned checkout; never use git stash or Git commands that change state; never delegate. Report exact files, metrics/raw output paths, commands/final gate lines, assumptions/unfinished work, branch and checkout read-only output. Parent alone reviews/transfers/cleans.
