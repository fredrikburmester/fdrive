# P5 scope verification: internal directory foundation

Bounded independent subchunk of P5-SCOPES. Own production/tests: services/indexer/src/fdrive_indexer/server.py, new directory_listing.py and tests/test_directory_listing.py, tests/test_server.py; apps/api/src/system/indexer-client.ts/test; packages/contracts/src/system.ts/test and index.ts exports. No composition/auth/web/other indexer changes. Primary owns full resolver architecture later. Worker direct implementation, no Git state changes/stash/delegation.

Internal indexer GET /directory?root=<configured-name>&path=<root-relative-canonical-path> returns {items:[{name:string,kind:'file'|'dir'|'symlink'|'other'}],overflow:boolean}. At most10000 entries; stop upon10001st and overflow true. Sort retained entries. Path uses leading slash rooted within selected mount ('/' root), never arbitrary OS absolute path. Strict reject repeated separators/dot/dotdot/backslash/NUL/control, missing/repeated/unknown query fields invalid400, unknown root404. Unicode/spaces/literal % names roundtrip one URL decode. Do not return filesystem absolute paths, contents, sizes, credentials or raw OS errors.

Keep existing internal-network-only trust boundary; indexer has NO existing secret/auth middleware (P5-SCOPES draft statement corrected). No new public fdrive directory route. Prevent symlink escapes/races: open trusted configured root directory, walk each descendant with dir_fd and O_DIRECTORY|O_NOFOLLOW; list via final directory descriptor. Root configuration itself may be resolved once as trusted root; user paths may not follow symlinks. List symlink entries as symlink without following them. Reject target symlink, non-directory, missing/permission errors safely. Close all descriptors/scandir iterators on success/error/overflow; offload blocking IO from Starlette async event loop. Do not touch index DB or originals.

Contracts export IndexerDirectoryResponse + inferred type, bounded names/items/kind/overflow. Existing IndexerClient gets directory(root,path):Promise<SidecarResult<IndexerDirectoryResponse>>, using sidecar transport/query encoding and strict response schema. Malformed/upstream unavailable fails consistently; no guessing empty list. Existing indexer mocks that explicitly implement interface may need minimal method stubs in API test fixtures; request exact ownership extension before changes.

Tests: real tmpdirs, correct kinds incl symlinks, unicode/percent/space, traversal, encoded separators/dot segments, outside-root symlink and intermediate swap race (fd guard), 10001 entries overflow, no unbounded scan, cleanup/error paths; API endpoint contract and TS client response/URL tests. Pure validators100%, overall Python>=95, ruff/mypy; relevant TS lint/typecheck/coverage gates. Docker Python test script per WORKING (real inotify). No dependency additions. Report exact files/gates/limitations and read-only Git branch/root.
