# Unified search UX

Plan: PLAN.md section9 search; user request2026-09-08. Model gemini-3.8-flash-high, high; role implementer.
Checkout /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-search-unified; branch claude/ux-search-unified; baseline e922bb4, no uncommitted prerequisites.
Own apps/web/src/components/search/, apps/web/src/lib/search/, apps/web/e2e/search.spec.ts and new search-specific e2e files. No other paths without reporting need.
Implement now: modestly larger desktop search dialog (roughly 800px wide, bounded viewport height), polished unified search: same query concurrently searches filenames, content/semantic and visual image content when available. Remove manual Images mode/toggle and session preference usage. File type Images remains a filter, clearly labeled as file type; filters stay visible. Text results must render without waiting for images; separate restrained visual matches section, deduplicate same identity/path already shown. Preserve keyboard navigation, open/reveal actions and mobile layout, loading/errors/partial notices. Image endpoint is active-identity only: label visual matches scope truthfully when all-logins selected, never imply cross-identity coverage. Apply folder/type filtering to returned visual hits, boundary-aware folder matching; acknowledge limited candidates if filtered locally. Skip visual request for non-image file types. Include active identity in image query cache key to avoid stale cross-identity hits. No auth/API/schema changes. Preserve existing text search ranking/contracts.
Tests: unified concurrent results, no manual mode, filters/folder boundary, dedup, image failure leaves text usable, loading, identity query keys, keyboard actions. Update affected browser tests. Run package @fdrive/web profile then affected browser search specs using exact absolute helper invocation; inspect all final output, don't lower gates.
Use bash /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-search-unified/tools/orchestration/verify.sh /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-search-unified package @fdrive/web
Custom commands must use bash /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-search-unified/tools/orchestration/run-in-checkout.sh /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-search-unified --lock -- ...
Read WORKING.md and relevant existing code. Reuse existing shadcn controls and theme tokens. Never delegate, never git stash, never run Git mutations. You are not alone: preserve other workers changes; edit assigned checkout/paths only. Primary owns docs, integration, commits. Report changed files, exact checks/final summaries, assumptions, unfinished work, branch/root. Do not spend time on orchestration changes.
