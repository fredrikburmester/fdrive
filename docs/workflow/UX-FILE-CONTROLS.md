# File controls UX

Plan: PLAN.md section9 file browser; user request2026-09-08. Model gemini-3.8-flash-high, high; role implementer.
Checkout /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-file-controls; branch claude/ux-file-controls; baseline e922bb4, no uncommitted prerequisites.
Own apps/web/src/components/files/, apps/web/src/lib/files/, apps/web/src/components/shell/app-sidebar.tsx and its tests, and new apps/web/e2e/file-controls.spec.ts. Do not edit search files or search.spec.ts, shared ui primitives, other paths without reporting need.
Implement: (1) Search enclosing-folder reveal must select/highlight actual item AND scroll into view even far outside virtual viewport, list/grid/tree, same folder and navigation. Existing reveal effect in file-browser queries mounted DOM then router.replace removes select; investigate remount/reset and use robust reveal lifecycle. Virtualizer must scrollToIndex before DOM row exists, retain selection after URL cleanup. Repeated reveal works and ordinary keyboard selection preserved. Search worker owns URL construction unchanged. (2) Download selected files toolbar action next to compress/duplicate, available with multi-selection and narrow-screen menu. Reuse exact existing single download/zip helper, error handling; no API change. (3) View menu persisted Show thumbnails option for list view (default off), one-line descriptive text. Reuse existing thumbnail request/fallback logic, compact fixed row sizes and virtualization; inaccessible/broken/non-image files show icons. (4) Decrease sidebar vertical nav item spacing modestly, scoped to app sidebar, preserve usable target sizes and hierarchy/mobile.
Tests: regressions for far-offscreen reveal surviving navigation and repeated same-folder reveal, multi-download toolbar invokes existing zip route, toggle persistence and thumbnail error fallback. Add browser coverage in owned new file; don't duplicate source implementation in assertions. Run package @fdrive/web then affected browser file-controls spec; do not lower coverage gates.
Use bash /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-file-controls/tools/orchestration/verify.sh /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-file-controls package @fdrive/web
Custom commands must use bash /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-file-controls/tools/orchestration/run-in-checkout.sh /Users/fredrikburmester/Documents/GitHub/fdrive-web/.worktrees/ux-file-controls --lock -- ...
Read WORKING.md, reuse existing shadcn/theme components. Never delegate, never git stash, never run Git mutations. You are not alone: preserve others changes, edit assigned checkout/paths only. Primary owns docs/integration/commits. Report changed files, exact checks/final summaries, assumptions, unfinished work, branch/root. No orchestration changes.
