# P4 edit admission: fail closed

Real ONLYOFFICE read-only coeditor can inject changes persisted by another writer. Explicit browser edit intent is NOT authority. Preserve credential-only authentication: production editing requires an explicit operator-maintained policy; default denies all editing. This is a second trusted authorization configuration, like index-root mapping, not automatic SFTPGo permission discovery. Operators must grant only a subset of current SFTPGo overwrite rights and close existing office sessions when changing rights. Document this limitation prominently and precisely.

## Fixed seam and policy, pending implementation

OfficeDeps may accept `canEdit?: (actor: OfficeActor, virtualPath: string) => Promise<boolean>`. Missing or errors => false. Callback checks current actor/session/provider and canonical path as before, then degrades claims.mode edit to view when not admitted; CheckFileInfo UserCanWrite/ReadOnly derives effective mode, never token intent alone. Each data mutation checks effective mode; destination paths for save-as/rename additionally require canEdit. Creating new office file checks target admission before bytes. No upstream mutation to infer capability.

Browser open mode edit denied: return403, no edit token, no editor session. Existing View request remains successful. HostEditUrl is omitted when denied, or points to host view URL if required by product; do not advertise an editable action. Production discovery/status may still enumerate supported formats; UI403 explains editing unavailable, View remains available. No production environment test bypass or permissive default.

Production config `FDRIVE_OFFICE_EDIT_RULES` is optional JSON, default []. Each strict rule is {providerId: canonical UUID, username: exact nonempty string <=255, path: strict canonical virtual path, recursive: boolean, allow: boolean}. Up to500 rules, input <=128KiB. No wildcard usernames/providers/paths, no credentials. Exact path matches; recursive true includes descendants only at segment boundaries. Longest matching path wins; deny wins ties; no match denies. Pure policy evaluator validates path and matches actor.identity.providerId + externalUsername, with no DB/network calls inside write transactions. Provider UUID binds rules to one configured SFTPGo server. Invalid JSON/rules fail startup. Document how operator obtains provider UUID from fdrive's own providers table, without SFTPGo admin credentials. Default env examples remain empty; isolated dev/test rules derive from known fixture rights.

Composition passes policy evaluator into OfficeDeps.canEdit. Optional server-injected officeCanEdit override in ComposeAppDeps is a dependency seam for isolated tests, not an environment bypass. Tests explicitly inject authority from fixture's known seeded permission definition (Alice/Bob fixture shared root only, Reader never); test mutable policy revocation. Missing OfficeDeps.canEdit and empty production config both deny. No test source imported by production. No auto-grant based on download, successful login, profile flags, or filesystem mode. Future authoritative upstream integration can replace this seam without changing callbacks.

Own files when assigned: office/types.ts/auth.ts/service.ts/writes.ts, new office/edit-policy.ts/tests, config.ts/tests plus office tests/test-fixtures and API office integration fixture. Composition seam queued until shares API finishes owning composition.ts. E2E fixture may add dedicated API entrypoint that imports composeApp and passes authorizer; never read browser-controlled header as authority. No other code paths/dependencies. Parent reviews all security code. Preserve regression where Reader requests edit while Alice saves; reader marker must never enter document. Also no-authorizer tests deny create/edit/token/mutations while view still works, callback authorization failures fail closed, destination-specific deny and revocation. Policy pure100%, nested deny and sibling-prefix tests, provider/username binding. Existing writer tests explicitly opt into known test authority. Parent handles PLAN, env example and deployment docs after review.
